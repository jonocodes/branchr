{
  "public": {
    "serviceName": "Test App",
    "localGitDir": "$LOCAL_GIT",
    "dockerBuildDir" : "$DOCKER_BUILD_DUIR",
    "dockerComposeFile": "/tmp/demo-compose.yml",
    "dockerBuildCmd": "docker build -t $IMAGE .",
    "host": "$DOCKER_HOST",

    "requiredPorts" : {
      "web" : [
        "PORT_HTTP_WEB"
      ],
      "redis" : [
        "PORT_TCP_REDIS"
      ]
    }
  }
}
